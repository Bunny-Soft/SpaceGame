NOTE:
	The values for each ship still need to be set, they're all set to zero right now. 
	Further, each variable of the ship can be set (this allows us to change anything if we need to).
		*Variables are protected. 